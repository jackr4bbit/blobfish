[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "blobfish"
version = "0.1.2"
description = "Python binary helper"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Jack Huey" }
]
dependencies = []

